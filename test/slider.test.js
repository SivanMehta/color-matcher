describe('Slider', function () {
  it('should use the correct color as a title');
  it('should have the correct color gradient range');
  it('should call the given on onChange when interacted with');
});
